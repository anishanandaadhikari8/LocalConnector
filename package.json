{
  "name": "circles",
  "version": "1.0.0",
  "description": "Neighbor Connect — Circle MVP for Apartments, Hotels, and Offices",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "npm run dev --workspace=apps/client",
    "build": "npm run build --workspace=apps/client",
    "start": "npm run start --workspace=apps/client",
    "lint": "npm run lint --workspaces",
    "type-check": "npm run type-check --workspace=apps/client"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "typescript": "^5.0.0"
  },
  "dependencies": {
    "ajv-keywords": "^3.5.2"
  }
}
